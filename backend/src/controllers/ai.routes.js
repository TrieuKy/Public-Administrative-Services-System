/**
 * ai.routes.js đặt trong controllers/ là file placeholder.
 * Route AI thật sự được định nghĩa tại src/routes/ai.routes.js
 * File này được giữ lại để tránh import error nếu có nơi nào require nó.
 */
const router = require('express').Router();
module.exports = router;
